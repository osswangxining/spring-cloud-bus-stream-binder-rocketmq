spring:
  application:
    name: testApp4RocketMQBus
  cloud:
    bus:
      enabled: true
      refresh:
        enabled: true
      trace:
        enabled: true    
management:
  security:
    enabled: false        